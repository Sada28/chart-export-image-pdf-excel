import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'
import LineChartapp from './Screen/GraphScreen/Linechart'

const App = () => {
  return (
    <SafeAreaView>
      <LineChartapp/>
    </SafeAreaView>
  )
}

export default App

const styles = StyleSheet.create({})